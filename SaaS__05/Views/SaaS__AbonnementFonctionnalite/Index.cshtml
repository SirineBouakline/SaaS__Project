@model IEnumerable<SaaS__05.Models.SaaS__Abonnement____88____SaaS__Fonctionnalite>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutBack.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SaaS__Abonnement.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SaaS__Fonctionnalite.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SaaS__Abonnement.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SaaS__Fonctionnalite.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_ }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_ }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_ })
        </td>
    </tr>
}

</table>
